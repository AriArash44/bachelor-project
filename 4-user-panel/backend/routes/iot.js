import { Router } from 'express';
import postData from '../utils/poster';
import db from '../utils/dbPool.js';

const router = Router();

router.post('/predict', (req, res) => {

})

export default router;